# マインスイーパー リリース手順書

| 項目 | 内容 |
|------|------|
| 工程 | 14. リリース準備 |
| 作成日 | 2026-09-26 |
| 状態 | ユーザーが承認した（2026-09-26）。2 章の決定は確認事項として挙げ、ユーザーは個別の回答をせずに承認したので、前例（docs/reviews/02-spec-review.md）に従い、推した案どおりに確定した |
| 入力 | docs/02-spec.md（仕様書 7 章・8 章）、docs/04-architecture.md（13 章）、docs/reviews/code-review.md（工程 13） |

## 1. 概要

Web 版の最初の版（1.0.0）を、GitHub Pages のプロジェクト サイトとして公開するための手順と確認項目をまとめる。

| 項目 | 内容 |
|------|------|
| 公開する版 | 1.0.0（git のタグは `v1.0.0`） |
| 公開先 | `https://fujiwo.github.io/Shos.Minesweeper/`（仕様書 7 章） |
| リポジトリ | `https://github.com/Fujiwo/Shos.Minesweeper`（公開リポジトリ。既定のブランチは `main`） |
| 公開の方法 | GitHub Actions のワークフロー（`.github/workflows/deploy.yml`）を、手動で起動する |
| 公開物 | `dotnet publish -c Release` の `wwwroot` を、サブパス用に整えたもの（3 章） |

公開の操作（push、ワークフローの起動、タグ付け）は、リリースレビューが承認され、ユーザーが明示的に指示したときだけ行う（CLAUDE.md）。

## 2. この工程で決めたこと

| # | 決めたこと | 理由 | 見送った案 |
|---|------------|------|------------|
| 1 | 公開は GitHub Actions で行い、ワークフローは手動でだけ起動する（`workflow_dispatch`） | 発行、`<base href>` の書き換え、配置を毎回同じ手順で行える。公開の前に全テストを流すので、Red のまま公開できない。push では動かないので、「公開はユーザーの指示のときだけ」（CLAUDE.md）を守れる | 手作業で発行して `gh-pages` ブランチに push する（手順を誤りやすい）。`main` への push で自動で公開する（指示のない公開になる） |
| 2 | 版は 1.0.0 とする | 仕様書の機能をすべて満たす最初の公開版である | 0.x（機能がそろっているので、試作の版にする理由がない） |
| 3 | ブラウザーのタブのアイコン（ファビコン）を、ゲームの地雷と同じ形の SVG（`wwwroot/favicon.svg`）にする。テンプレートの `icon-192.png`（Blazor のロゴ。どこからも参照されていない）は消す | 公開する物にテンプレートのロゴを残さない。形をゲームのアイコンとそろえ、新しい絵を足さない。SVG なので大きさによらずにじまない（工程 12 で、扱いを工程 14 で決めると送った事項） | PNG の各サイズとホーム画面用のアイコンを作る（PWA は対象外なので要らない。仕様書 9 章） |
| 4 | 公開物に `404.html`（`index.html` の写し）を置く | GitHub Pages は、無い URL に GitHub の英語の 404 ページを返す。`404.html` を置くと、無い URL でもアプリが読み込まれ、アプリの日本語の「ページが見つかりません」（工程 11 の区切り 7 で作ったもの）が出る | 置かない（誤った URL を開いた人に、英語のページが出る） |
| 5 | 公開物に `.nojekyll` を置く | `_framework` のように `_` で始まるフォルダーを、Jekyll の処理にかけずにそのまま配るため（調査書 8.5）。Actions で配置する場合は Jekyll は動かないが、手作業で配置し直すことになっても同じ公開物で済むように置く | — |
| 6 | 発行が作る圧縮ファイル（`.br`・`.gz`）は、そのまま公開物に含める。書き換えた `index.html` の圧縮ファイルだけは消す | GitHub Pages は自分で gzip に圧縮して配るので、これらは使われないが、害もない。発行の設定を増やさない。`index.html` の圧縮ファイルは書き換える前の中身なので、残すと食い違う | 発行の設定で圧縮を止める（設定が増える割に、得るものがない） |

## 3. 公開物の構成

ワークフローの「公開用に整える」の手順で、発行した `wwwroot` を次のように整える。

| 対象 | 整え方 | 確かめ方 |
|------|--------|----------|
| `index.html` の `<base href>` | `/` を `/Shos.Minesweeper/` に書き換える。ソースの `index.html` は `/` のまま（ローカルの `dotnet run` のため。アーキテクチャー設計書 13 章） | 書き換えた後に `grep` で確かめ、無ければワークフローを失敗させる |
| `index.html.br`、`index.html.gz` | 消す（2 章の決定 6） | — |
| `404.html` | 書き換えた `index.html` を写す（2 章の決定 4） | — |
| `.nojekyll` | 空のファイルを置く（2 章の決定 5） | — |

そのほかに、ビルドが済ませていること:

- `index.html` のプレースホルダー（`<script type="importmap">`、`<link rel="preload" id="webassembly">`、`blazor.webassembly#[.{fingerprint}].js`）は、発行のときに .NET 10 が書き換える（CLAUDE.md の「構成とポイント」）。
- `js/browser.js` は相対パスで読み込むので、サブパスでも読み込める（アーキテクチャー設計書 16 章）。

大きさ（2026-09-26 の発行物）: 最初の読み込みで取る `_framework` は、圧縮しないと約 9.3MB、GitHub Pages が配る gzip で約 3.6MB である。

ローカルでの事前の確認（工程 14 で行った）: 発行物をワークフローと同じ手順で整え、GitHub Pages と同じく「サブパスの下でファイルを配り、無いパスには `404.html` を 404 の状態で返す」小さなサーバー（作業用のスクリプト）で配った。ヘッドレスの Chrome で、アプリが読み込まれてマスを開けること、`favicon.svg` が `image/svg+xml` で配られること、`/Shos.Minesweeper/no-such-page` と `/Shos.Minesweeper/a/b/c` でアプリの「ページが見つかりません」が出ることを確かめた。

## 4. 公開の手順（工程 16）

前提: リリースレビュー（工程 15）が承認され、ユーザーが公開を指示している。

| # | 手順 | 行う人 | 確かめること |
|---|------|--------|--------------|
| 1 | 公開する変更をコミットし、`main` を `origin` に push する（コミットと push はユーザーの指示で行う） | Claude（指示を受けて）かユーザー | `origin/main` に `.github/workflows/deploy.yml` がある |
| 2 | GitHub のリポジトリの Settings → Pages → Build and deployment → Source を「GitHub Actions」にする（初回だけ） | ユーザー | Pages の設定画面に、公開先の URL が表示される |
| 3 | Actions → 「GitHub Pages に公開」→ Run workflow（ブランチは `main`）で起動する | ユーザー（または、ユーザーの指示で Claude） | `build`（テスト・発行・整える・アップロード）と `deploy` が成功する。テストの件数が、ローカルの `dotnet test` と同じ |
| 4 | 公開後の確認（6 章）を行う | Claude とユーザー | 6 章の表のすべて |
| 5 | 確認が済んだら、公開したコミットに `v1.0.0` のタグを付けて push する（ユーザーの指示で行う） | Claude（指示を受けて）かユーザー | `origin` に `v1.0.0` のタグがある |
| 6 | 公開後の確認の結果を、docs/reviews/06-release-review.md に追記する | Claude | — |

- ワークフローで使う Actions の版（`actions/checkout@v4`、`actions/setup-dotnet@v4`、`actions/upload-pages-artifact@v3`、`actions/deploy-pages@v4`）は、2026-09-26 に書いたものである。手順 3 で「古い版」の警告が出たら、その版を上げてから起動し直す。
- 手順 3 のワークフローが失敗したら、公開はされない（前の状態のまま）。ログを見て原因を直し、工程 15 の確認項目のうち、直した部分に関わるものを確かめ直してから起動し直す。

## 5. 確認項目（リリースレビュー、工程 15）

### 5.1 テスト

| 項目 | 確かめ方 |
|------|----------|
| 全テストが Green | リポジトリ直下で `dotnet test`。2026-09-26 の時点で 382 件 |
| ビルドの警告がない | `dotnet build Shos.Minesweeper.slnx` と `dotnet publish Shos.Minesweeper -c Release` で、警告 0 |

### 5.2 仕様書の受け入れ基準（仕様書 8 章）

| # | 受け入れ基準 | 主な根拠 |
|---|--------------|----------|
| 1 | 初級・中級・上級・カスタムで、勝利と敗北の両方までゲームを進められる | GameLogic と `GamePage` のテスト。工程 13 で、初級〜上級を本物のクリックで勝ち、推測で負けた。カスタム（12×10、地雷 15）はリリースレビューで勝ちと負けまで進めた（docs/reviews/06-release-review.md） |
| 2 | 最初に開いたマスが、どの難易度でも必ず 0 になる | `GameTests`（端・角・中央、本物の乱数で繰り返す） |
| 3 | コード、旗、旗モード、長押しが 4 章の表のとおりに動く | `PressMappingTests`（すべての組み合わせ）、`BoardViewPointerTests`、工程 13 のタッチの確認と実機の確認 |
| 4 | 各画面で盤面が 5.2 の規則どおりに表示される。長押しでズーム・文字選択・メニューが出ない | `BoardPlacementTests`（UI デザイン 3.3 の表）、工程 11 の 48 通りの画面の確認、工程 13 の実機の確認 |
| 5 | キーボードだけでゲームを最後まで進められる | `BoardViewKeyboardTests`、工程 11 の区切り 5 のブラウザーでの確認 |
| 6 | ベストタイムが記録され、ページを開き直しても残る | 工程 13（3 つの難易度で勝ち、開き直して確かめた） |
| 7 | ライトとダークの両方の配色で、数字が読める | 工程 11 の区切り 7 の写真、工程 13 の実機の確認 |
| 8 | 旗モードでも開いた数字のマスのタップでコード。マウスの左ボタンの押し続けで旗にならない | `PressMappingTests`、`PressGestureTests` |
| 9 | プレイ中に画面の向きを変えても、ゲームが続く | `BoardAreaTests`、工程 13 の実機の確認 |
| 10 | 保存ができない環境でも、最後まで遊べる | `BestTimeStorageTests`（値がないとき）、`BestTimesJsonTests`、`browser.js` が例外を受け止める（アーキテクチャー設計書 10 章）。リリースレビューで、localStorage が例外を投げる状態にして、2 回続けて勝つまで遊んだ（docs/reviews/06-release-review.md） |
| 11 | 未開始のうちに旗を立てても、最初に開いたマスが 0 になる | `GameTests` |
| 12 | 実機のスマートフォンで、上級の盤面の操作が 100 ミリ秒以内（体感） | 工程 13 の実機の確認（ユーザー。遅れを感じなかった）。ヘッドレスの計測は docs/reviews/code-review.md の工程 13 |
| 13 | スクリーンリーダーで、マスの位置と状態、勝敗が読み上げられる | `CellPresentationTests`、`AnnouncementsTests`、工程 13 の実機の確認（ユーザー） |

### 5.3 動作環境の実機の確認（ユーザー）

工程 13 で確かめた（docs/reviews/code-review.md の工程 13）。リリースレビューでは、工程 13 の後にアプリの見た目や操作に関わる変更（ファビコンの追加など）が、実機での振る舞いを変えていないかを見る。変えていれば、その部分を確かめ直す。

| 端末 | ブラウザー |
|------|------------|
| スマートフォン（iOS） | Safari |
| スマートフォン（Android） | Chrome |
| タブレット | 各 OS の標準のブラウザー |
| PC | Chrome、Edge、Firefox（Mac なら Safari） |

### 5.4 公開物の設定

| 項目 | 確かめ方 |
|------|----------|
| `<base href>` が `/Shos.Minesweeper/` になる | ワークフローの「公開用に整える」の `grep`。ローカルでは 3 章の事前の確認 |
| `404.html`、`.nojekyll` がある | 同上 |
| ソースの `index.html` の `<base href>` は `/` のまま | `git diff` で `index.html` を見る |
| ファビコンが出る | 3 章の事前の確認。公開後に、ブラウザーのタブで見る |
| ワークフローは手動でだけ動く | `deploy.yml` の `on:` が `workflow_dispatch` だけ |
| 権限は要るジョブにだけ与える | `deploy.yml` の既定の権限は `contents: read` だけで、`pages: write`・`id-token: write` は `deploy` のジョブだけにある |

## 6. 公開後の確認（工程 16）

| # | 確かめること | 端末 |
|---|--------------|------|
| 1 | `https://fujiwo.github.io/Shos.Minesweeper/` を開くと、読み込み中の表示の後に初級の盤面が出る | PC とスマートフォン |
| 2 | ブラウザーのタブに、題名「マインスイーパー」とファビコンが出る | PC |
| 3 | マスを開き、旗を立て、勝つか負けるまで遊べる。難易度を変えられる | PC とスマートフォン |
| 4 | 勝ってベストタイムが記録され、開き直しても残る | PC |
| 5 | `https://fujiwo.github.io/Shos.Minesweeper/no-such-page` で、アプリの「ページが見つかりません」が出て、「マインスイーパーに戻る」でゲームに戻れる | PC |
| 6 | ブラウザーの開発者ツールで、読み込みのエラー（404 など）が出ていない | PC |

## 7. 元に戻す方法

| 場合 | 方法 |
|------|------|
| 公開した版に問題があり、前の版に戻したい | Actions の「GitHub Pages に公開」を、前の版のタグ（`v1.0.0` の後なら、その前のタグ）を選んで起動する。ワークフローは、選んだ版のソースから発行し直す |
| 最初の公開（1.0.0）を取り下げたい | Settings → Pages で、サイトの公開を止める（Unpublish site） |

## 8. 公開の後の予定

Web 版の公開（工程 16）の後に、同じゲームの WPF 版とコンソール版を、新しい一巡として作る（CLAUDE.md の「目的」）。このワークフローは Web 版だけを公開する。
