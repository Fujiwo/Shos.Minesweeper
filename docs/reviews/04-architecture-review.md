# アーキテクチャー設計書レビュー

| 項目 | 内容 |
|------|------|
| 工程 | 8. アーキテクチャー設計書レビュー |
| 対象 | docs/04-architecture.md |
| レビュー日 | 2026-09-25 |
| レビュー者 | Claude（自己レビュー） |
| 状態 | 指摘をすべて反映済み。ユーザーの承認待ち |

## 観点

CLAUDE.md のとおり、sustainable-code-jp スキルの七箇条と Think Simple（引き算の設計）を観点にする。加えて、前の工程の成果物との整合を見る。

| 観点 | 問い |
|------|------|
| 意図を表現 | 各単位の仕事が What で書かれ、How（実装の細部）が前に出ていないか |
| 単一責務 | 各単位の変更理由は一つか |
| 的確な名前 | 名前が仕事を過不足なく表しているか。要求の語彙と対応しているか |
| Once And Only Once | 同じ規則や値が二箇所に置かれていないか |
| 的確なメソッド | （この工程ではメソッドを決めないので、対象外） |
| ルールの統一 | 既存のテンプレートの流儀、CLAUDE.md の決まりに合っているか |
| Testable | 正しいことを確かめられる形か。差し替え口は足りているか |
| 引き算 | 頼まれていない抽象・仕組み・備えがないか。問題文にない制約を足していないか |
| 整合 | 仕様書・UI デザインと矛盾しないか。書かれた方式で、要求を実際に実現できるか |

重大度の基準は、調査書レビュー（docs/reviews/01-research-review.md）と同じとする。

## ユーザーの確認事項の扱い

設計書の提出時に、17 章で「テストで時刻を進めるための偽物に `FakeTimeProvider`（`Microsoft.Extensions.TimeProvider.Testing` パッケージ）を使うか」を確認事項として挙げた。ユーザーは個別の回答をせずに工程を承認したので、仕様書レビューの前例（docs/reviews/02-spec-review.md）に従い、**推した案どおりに確定した**ものとして扱う。設計書の 12 章と 17 章をそのように書き換えた。

## 指摘

| # | 重大度 | 観点 | 場所 | 指摘 | 対応 |
|---|--------|------|------|------|------|
| 1 | 中 | 整合 | 7.1 | 「ダイアログ・勝利カードの表示中は、ほかの部分を `inert` にする」とある。UI デザイン 2.4 では勝利カードはモードレスであり、`inert` にするとリセット ボタンなども押せなくなって矛盾する | `inert` にするのは難易度ダイアログを表示している間だけにした |
| 2 | 中 | Testable、意図を表現 | 6 章 | DI に何を登録し、どのオブジェクトを誰が作るのか（組み立て）が書かれていない。`TimeProvider` を差し替え口にすると決めたのに、コンポーネントまでどう届くかが分からない。このままでは、bUnit のテストで偽物の時刻を渡せるかどうかを判断できない | 6.4「組み立て」を追加した。DI に登録するのは `TimeProvider`、`BrowserFeatures`、`BestTimeStorage` の 3 つだけとし、状態を持つオブジェクトは持ち主のコンポーネントが作ることにした |
| 3 | 中 | 整合 | 6.3、9.2 | 長押しの円は `BoardArea` の中に描く。盤面の領域がスクロールするときは領域の端で円が切れ、UI デザイン 5.2 の「盤面の端のマスでも円の全体が見える」を満たせない。また、画面の端のマスでは、どう描いても円が画面の外に出る | 円を画面に固定した層（`position: fixed`）に描き、位置はポインターのイベントの `ClientX − OffsetX` から求めることにした（JavaScript は要らない）。画面の端で円が切れることは受け入れ、UI デザイン 5.2 の記述をそのように直した |
| 4 | 中 | 整合、単一責務 | 6.2 | `BoardCursor` は「表示の向きで動く」とあり、位置を表示の座標で持つと読める。画面の向きが変わって盤面の縦と横が入れ替わると、選んでいたマスとは別のマスを指してしまう（仕様書 5.3 は、向きが変わってもそのまま続けることを求めている） | 位置は盤面の座標で持ち、矢印キーの方向を `BoardPlacement` で盤面の方向に変えてから動かすことにした |
| 5 | 軽微 | 整合 | 8.1 | マウスで押したまま盤面の外に出てボタンを離すと、`pointerup` が盤面に届かず、押下中の表示が残る | 盤面の外に出たら（`pointerleave`）取り消すことにし、状態の遷移図にも書き足した |
| 6 | 軽微 | Testable | 7 章 | `ResizeObserver` の監視、JavaScript に渡す .NET の参照、長押しの待ち、1 秒ごとのタイマーを、いつ止めるかが書かれていない。bUnit のテストでは、テストのたびにコンポーネントを作って破棄するので、止めないとタイマーが残る | 7.4「後片付け」を追加した |
| 7 | 軽微 | 的確な名前 | 4 章、6.3 ほか | 唯一のページの名前 `Home` は、テンプレートでの位置（ホーム）を表すだけで、仕事（ゲームの画面）を表していない。この設計書では、このページに状態の持ち主という大きな役割を持たせるので、名前で仕事が分かるほうがよい | `GamePage` に変える（ルートは `/` のまま）。設計書の中の `Home` をすべて置き換えた |
| 8 | 軽微 | 意図を表現 | 12 章、17 章 | 確認事項（時刻の偽物）が未確定のままの書き方になっている | 「ユーザーの確認事項の扱い」のとおり確定し、書き方を改めた |

## 引き算の点検

新しく足した単位と仕組みについて、「今日の問題の何を解いているか」を点検した。削ったもの・足さなかったものはない。

| 単位・仕組み | 解いている問題 | 判定 |
|--------------|----------------|------|
| GameLogic のプロジェクトを分ける | ルールが UI に依存しないことを、コンパイラーで守る（CLAUDE.md の設計方針） | 残す |
| `PressGesture` と `InputMapping` を分ける | 変更理由が違う（判定の時間と距離／旗モードでの割り当て） | 残す |
| `BrowserFeatures` と `BestTimeStorage` を分ける | JavaScript の呼び出しと、保存の形式・失敗の扱いは、変更理由が違う | 残す |
| `ElapsedTime` を別のコンポーネントにする | 毎秒の描き直しで盤面を描き直さない（仕様書 6.2 の性能） | 残す |
| キーのスクロールを抑える JavaScript | Blazor ではキーごとに既定の動作を止められない | 残す（Blazor だけでは実現できない） |
| インターフェイス | 作っていない（15 章） | 妥当 |

- 指摘 2 で足した 6.4 は、新しい仕組みではなく、既に決めた差し替え口（`TimeProvider`）の届け方を書いたものである。DI に登録するのは、差し替える必要があるものと JavaScript を呼ぶものだけにした。
- 指摘 3 で、円の位置を JavaScript（`getBoundingClientRect`）で求める案も考えた。イベントの `ClientX − OffsetX` で求められるので、JavaScript を足さずに済ませた。

## 良い点

- 関心事の一覧（3 章）が先にあり、クラスはその結果として決まっている。各単位の「ひとことで言うと」と「持たないもの」が書かれていて、責務の境界が読み取れる。
- 盤面の縦と横の入れ替えを GameLogic に持ち込まず、`BoardPlacement` に閉じ込めている。ルールと表示という独立したものを、独立したまま表している。
- 上バーと横バーの切り替えを CSS だけに任せ、C# は測った領域の大きさだけを使う。レイアウトの規則が二重に書かれない（Once And Only Once）。
- JavaScript を使うものと使わないものを、理由とともに表にしている。JavaScript を使うのは 4 つだけで、C# の窓口も 1 つにまとまっている。
- 作らないもの（15 章）に理由が書かれていて、後から「なぜ無いのか」を問われたときに答えられる。

## 残る課題

- `BoardView` は、マスを描くことに加えて、ポインターとキーボードの入力を意図に変える流れも受け持つ。判定そのものは `PressGesture`・`InputMapping`・`BoardCursor` に出してあるので今は許容するが、クラス設計（工程 9）で、マスの見た目（状態から CSS のクラスや読み上げの名前を決める部分）を別の C# のクラスに出すかどうかを判断する。
- 16 章のリスク（iOS の長押し、性能、`browser.js` の読み込み）は、実装と結合テストで確かめる。
